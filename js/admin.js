jQuery(document).ready(function($){

/*
$('.submit_button').click(function(event){

	var full_arr = new Array();
console.log( '----------------' );
	$('.id_list').each( function(){
	console.log( $('#tag_'+$(this).attr('data-id') ).attr('checked') );
	
		if( $('#tag_'+$(this).attr('data-id') ).attr('checked') == 'checked' )
			full_arr.push( { id: $(this).attr('data-id') } );
	} )
	console.debug( full_arr );
	var jsonString = JSON.stringify( full_arr );
	console.log( jsonString );
	$( '#el_array' ).val( jsonString  );

})
*/


}); // main jquery container
